@startuml
skinparam class {
  BackgroundColor White
  ArrowColor Black
}

class User {
  - id: String
  - name: String
  - email: String
  - password: String
  + createUser(): void
}

class Account {
  - id: String
  - userId: String
  - balance: BigDecimal
  - status: String
  + createAccount(): void
  + updateBalance(amount: BigDecimal): void
}

class Transfer {
  - id: String
  - fromAccountId: String
  - toAccountId: String
  - amount: BigDecimal
  - status: String
  + initiateTransfer(): void
}

class Transaction {
  - id: String
  - accountId: String
  - amount: BigDecimal
  - type: String
  - description: String
  + recordTransaction(): void
}

class Notification {
  - id: String
  - userId: String
  - message: String
  - status: String
  + sendNotification(): void
}

User "1" *-- "0..*" Account : owns
Account "1" *-- "0..*" Transaction : has
Transfer "1" -- "1" Account : from
Transfer "1" -- "1" Account : to
Transfer "1" -- "0..*" Transaction : creates
Notification "1" -- "1" User : for
@enduml